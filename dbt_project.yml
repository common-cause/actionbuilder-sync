name: 'actionbuilder_sync'
version: '1.0.0'
config-version: 2
profile: 'actionbuilder_sync'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  actionbuilder_sync:
    +materialized: view
